# requirements.txt — Geopolitical News Intelligence System (MVP v1)
# Python 3.12.2 | Windows | Local-first | Zero external paid APIs

# ── RSS Ingestion ──────────────────────────────────────────────────────
feedparser>=6.0.0

# ── HTML Cleaning & Boilerplate Removal ──────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=4.9.0
trafilatura>=1.6.0
newspaper3k>=0.2.8            # fallback boilerplate remover

# ── Text Processing ──────────────────────────────────────────────────────
spacy>=3.7.0                  # sentence segmentation (primary)
# After pip install, run: python -m spacy download en_core_web_sm
nltk>=3.8.0                   # sentence segmentation (fallback)

# ── Embeddings ─────────────────────────────────────────────────────────
sentence-transformers>=2.2.0  # all-MiniLM-L6-v2
numpy>=1.24.0

# ── Classification ─────────────────────────────────────────────────────
transformers>=4.30.0          # distilbert NLI model via zero-shot pipeline
# torch is managed by conda (Anaconda environment) — do NOT install via pip.
# Run: conda install pytorch cpuonly -c pytorch

# ── Clustering ─────────────────────────────────────────────────────────
# scikit-learn 1.3+ ships HDBSCAN built-in with pre-built wheels (no C++ compiler needed).
# The standalone 'hdbscan' package has no Python 3.12 wheel and fails to compile on Windows.
scikit-learn>=1.3.2

# ── Utilities ──────────────────────────────────────────────────────────
requests>=2.28.0              # used internally by trafilatura/newspaper
python-dateutil>=2.8.0
